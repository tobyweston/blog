---
---

<div class="blog-gallery">
  <slot />
</div>

<style>
  .blog-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin: 2rem 0;
  }

  .blog-gallery :global(figure) {
    margin: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .blog-gallery :global(img) {
    width: 100%;
    height: auto;
    border-radius: 6px;
    object-fit: cover;
  }

  .blog-gallery :global(figcaption) {
    margin-top: 0.4rem;
    font-size: 0.8em;
    color: rgb(var(--gray));
    text-align: center;
  }
</style>
